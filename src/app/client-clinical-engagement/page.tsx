import { ClinicalEngagement } from "@/components/solutions/ClinicalEngagement";

export const metadata = {
  title: "Clinical Engagement Engine — Credify",
  description: "Automate screenings, track client progress with automatic graphing, and loop in supervisors the moment clinical thresholds are crossed.",
};

export default function ClinicalEngagementPage() {
  return <ClinicalEngagement />;
}
