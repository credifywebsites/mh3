import { FrontDesk } from "@/components/solutions/FrontDesk";

export const metadata = {
  title: "Front Desk — StayingBooked by Credify",
  description:
    "Real, trained receptionists answer every call for your behavioral health practice. $1 per call, capped at $8/hour. Full coverage or overflow — 24/7/365.",
};

export default function FrontDeskPage() {
  return <FrontDesk />;
}
