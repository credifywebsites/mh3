import { ProviderEngagement } from "@/components/solutions/ProviderEngagement";

export const metadata = {
  title: "Provider Engagement Engine — Credify",
  description: "Automated compliance, note chasing, and treatment plan reviews for behavioral health providers. Supervisors stay free, nothing falls through.",
};

export default function ProviderEngagementPage() {
  return <ProviderEngagement />;
}
