import { Rcm } from "@/components/solutions/Rcm";

export const metadata = {
  title: "RCM Services — Credify",
  description:
    "Full-cycle revenue cycle management for behavioral health — AI-assisted billing, eligibility, denial follow-up, payment posting, and credentialing included. From 3.49% of collections.",
};

export default function RcmPage() {
  return <Rcm />;
}
