import { AppointmentEngagement } from "@/components/solutions/AppointmentEngagement";

export const metadata = {
  title: "Appointment Engagement Engine — Credify",
  description: "Recover lost revenue with AI-powered appointment confirmations, rescheduling, and no-show prevention.",
};

export default function AppointmentEngagementPage() {
  return <AppointmentEngagement />;
}
