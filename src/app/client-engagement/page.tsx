import { ClientEngagement } from "@/components/solutions/ClientEngagement";

export const metadata = {
  title: "Client & Provider Engagement Engine — Credify",
  description: "Automated appointment reminders, lapsed client recovery, and clinical progress tracking. Cut no-shows by 88%+ and recover lost revenue on autopilot.",
};

export default function ClientEngagementPage() {
  return <ClientEngagement />;
}
